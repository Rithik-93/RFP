# ========================================
# SERVICE PORTS
# ========================================

# AI Engine Port
ENGINE_PORT=3000

# Gateway API Port
GATEWAY_PORT=3001

# Engine URL (used by Gateway to communicate with Engine)
ENGINE_URL=http://localhost:3000

# ========================================
# DATABASE CONFIGURATION
# ========================================

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: postgresql://rfpuser:password123@localhost:5432/rfp_db
DATABASE_URL=postgresql://user:password@localhost:5432/rfp_management

# ========================================
# AI CONFIGURATION
# ========================================

# Google Gemini API Key
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ========================================
# EMAIL CONFIGURATION - SENDING (SMTP)
# ========================================

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com

# Gmail App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
# Note: You need 2FA enabled to create App Passwords
SMTP_PASS=your_16_character_app_password

# ========================================
# EMAIL CONFIGURATION - RECEIVING (Gmail API)
# ========================================

# OAuth2 Client ID from Google Cloud Console
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# OAuth2 Client Secret
GOOGLE_CLIENT_SECRET=your_oauth_client_secret

# OAuth2 Redirect URI (must match what's configured in Google Console)
# Update with your ngrok URL or production domain
GOOGLE_REDIRECT_URI=https://your-ngrok-url.ngrok-free.app/auth/callback

# OAuth2 Refresh Token (generated after first auth flow)
GOOGLE_REFRESH_TOKEN=your_refresh_token_here

# Gmail Pub/Sub Topic Name for push notifications
# Format: projects/YOUR_PROJECT_ID/topics/YOUR_TOPIC_NAME
TOPIC_NAME=projects/your-project-id/topics/your-topic-name

# ========================================
# APPLICATION CONFIGURATION
# ========================================

# Node environment
NODE_ENV=development

# ========================================
# NOTES
# ========================================
# 
# 1. Gmail SMTP Setup:
#    - Go to Google Account → Security → 2-Step Verification (enable it)
#    - Go to App Passwords: https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Other (Custom name)"
#    - Copy the 16-character password to SMTP_PASS
#
# 2. Gmail API Setup:
#    - Go to Google Cloud Console: https://console.cloud.google.com
#    - Create a new project or select existing
#    - Enable Gmail API
#    - Create OAuth 2.0 credentials (Web application)
#    - Add your redirect URI to Authorized redirect URIs
#    - Copy Client ID and Client Secret
#    - Run the OAuth flow once to get the refresh token
#
# 3. Database Setup:
#    - Install PostgreSQL or use hosted service (Neon, Supabase, etc.)
#    - Create a new database: CREATE DATABASE rfp_management;
#    - Update DATABASE_URL with your credentials
#    - Run: cd packages/database && pnpm run db:push
#
# 4. Gemini API Key:
#    - Go to: https://aistudio.google.com/app/apikey
#    - Create a new API key
#    - Copy to GEMINI_API_KEY
#
# 5. Gmail Pub/Sub Topic:
#    - Create a topic in Google Cloud Console → Pub/Sub
#    - Grant Gmail permissions to publish to the topic
#    - Use format: projects/PROJECT_ID/topics/TOPIC_NAME
#
# 6. Security:
#    - NEVER commit .env with real credentials to Git
#    - Rotate credentials regularly
#    - Use different credentials for dev/prod
#
